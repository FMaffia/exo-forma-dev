import React from 'react';
import {FormProps} from "../../model/models";


const FormProgetto = ({currentProject}: FormProps) => {
    return (
        <div>
        </div>
    );
};

export default FormProgetto;